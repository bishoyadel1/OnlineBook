@model  NewRole
@{
    ViewData["Title"] = "CreateRole";
}
<hr /><hr /><hr />
<h1>CreateRole</h1>
<div class="container">
    <div class="row">

            <form asp-action="createrole" method="post">
                        <div class="form-group">
                            <input type="text" hidden class="form-control" asp-for="RoleId" id="roleId" value="@(Model.RoleId!=null ? Model.RoleId : 1 )"  />
                            <label asp-for="RoleName" class="control-lable">Role Name</label>
                            <input type="text" class="form-control" asp-for="RoleName" id="roleName"  value="@(Model.RoleId!=null ? Model.RoleName :string.Empty)"/>
                            <span asp-validation-for="RoleName" class="text-danger"></span>
                        </div>

                        <div class="modal-footer-rtl" style="padding:0 15px 15px 0">
                            <input type="submit" class="btn btn-success" value="Save" id="btnSave" />
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        </div>
                    </form>

                            </div>
                            </div>